.side-groups
  - current_user.groups.each do |group|
    .side-groups__group
      = link_to group_messages_path(group), class: "side-groups__group" do
        .side-groups__group--group-name
          = group.name
        .side-groups__group--latest-message
          = group.show_last_message